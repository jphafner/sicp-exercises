

; iteratively sum
;(define (sum term a next b)
;  (define (iter a result)
;  (iter <??> <??>))
;    (if (<??>)
;        (<??>)
;        (iter <??> <??>)))
;  (iter <??> <??>))

; NOTE: I accidently did this for ex1-29


